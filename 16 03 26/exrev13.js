//Crie um programa que verifique se a pessoa pode dirigir, usando operador ternário.

let idade = 18;
let resultado = (idade >= 18) ? "Pode dirigir" : "Não pode dirigir";
console.log(resultado);
